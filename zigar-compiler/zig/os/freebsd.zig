const std = @import("std");

pub const target = std.os.freebsd;

pub fn with(comptime substitutes: anytype) type {
	return struct {
		pub const tokenizer = if (@hasDecl(substitutes, "tokenizer")) substitutes.tokenizer else std.os.freebsd.tokenizer;
		pub const Token = if (@hasDecl(substitutes, "Token")) substitutes.Token else std.os.freebsd.Token;
		pub const Tokenizer = if (@hasDecl(substitutes, "Tokenizer")) substitutes.Tokenizer else std.os.freebsd.Tokenizer;
		pub const versionCheck = if (@hasDecl(substitutes, "versionCheck")) substitutes.versionCheck else std.os.freebsd.versionCheck;
		pub const AF = if (@hasDecl(substitutes, "AF")) substitutes.AF else std.os.freebsd.AF;
		pub const ARCH = if (@hasDecl(substitutes, "ARCH")) substitutes.ARCH else std.os.freebsd.ARCH;
		pub const AT = if (@hasDecl(substitutes, "AT")) substitutes.AT else std.os.freebsd.AT;
		pub const CLOCK = if (@hasDecl(substitutes, "CLOCK")) substitutes.CLOCK else std.os.freebsd.CLOCK;
		pub const CPU_COUNT = if (@hasDecl(substitutes, "CPU_COUNT")) substitutes.CPU_COUNT else std.os.freebsd.CPU_COUNT;
		pub const E = if (@hasDecl(substitutes, "E")) substitutes.E else std.os.freebsd.E;
		pub const Elf_Symndx = if (@hasDecl(substitutes, "Elf_Symndx")) substitutes.Elf_Symndx else std.os.freebsd.Elf_Symndx;
		pub const F = if (@hasDecl(substitutes, "F")) substitutes.F else std.os.freebsd.F;
		pub const FD_CLOEXEC = if (@hasDecl(substitutes, "FD_CLOEXEC")) substitutes.FD_CLOEXEC else std.os.freebsd.FD_CLOEXEC;
		pub const F_OK = if (@hasDecl(substitutes, "F_OK")) substitutes.F_OK else std.os.freebsd.F_OK;
		pub const Flock = if (@hasDecl(substitutes, "Flock")) substitutes.Flock else std.os.freebsd.Flock;
		pub const HOST_NAME_MAX = if (@hasDecl(substitutes, "HOST_NAME_MAX")) substitutes.HOST_NAME_MAX else std.os.freebsd.HOST_NAME_MAX;
		pub const IFNAMESIZE = if (@hasDecl(substitutes, "IFNAMESIZE")) substitutes.IFNAMESIZE else std.os.freebsd.IFNAMESIZE;
		pub const IOV_MAX = if (@hasDecl(substitutes, "IOV_MAX")) substitutes.IOV_MAX else std.os.freebsd.IOV_MAX;
		pub const IPPROTO = if (@hasDecl(substitutes, "IPPROTO")) substitutes.IPPROTO else std.os.freebsd.IPPROTO;
		pub const LOCK = if (@hasDecl(substitutes, "LOCK")) substitutes.LOCK else std.os.freebsd.LOCK;
		pub const MADV = if (@hasDecl(substitutes, "MADV")) substitutes.MADV else std.os.freebsd.MADV;
		pub const MAP = if (@hasDecl(substitutes, "MAP")) substitutes.MAP else std.os.freebsd.MAP;
		pub const MSF = if (@hasDecl(substitutes, "MSF")) substitutes.MSF else std.os.freebsd.MSF;
		pub const MMAP2_UNIT = if (@hasDecl(substitutes, "MMAP2_UNIT")) substitutes.MMAP2_UNIT else std.os.freebsd.MMAP2_UNIT;
		pub const MSG = if (@hasDecl(substitutes, "MSG")) substitutes.MSG else std.os.freebsd.MSG;
		pub const NAME_MAX = if (@hasDecl(substitutes, "NAME_MAX")) substitutes.NAME_MAX else std.os.freebsd.NAME_MAX;
		pub const O = if (@hasDecl(substitutes, "O")) substitutes.O else std.os.freebsd.O;
		pub const PATH_MAX = if (@hasDecl(substitutes, "PATH_MAX")) substitutes.PATH_MAX else std.os.freebsd.PATH_MAX;
		pub const POLL = if (@hasDecl(substitutes, "POLL")) substitutes.POLL else std.os.freebsd.POLL;
		pub const PROT = if (@hasDecl(substitutes, "PROT")) substitutes.PROT else std.os.freebsd.PROT;
		pub const REG = if (@hasDecl(substitutes, "REG")) substitutes.REG else std.os.freebsd.REG;
		pub const RLIM = if (@hasDecl(substitutes, "RLIM")) substitutes.RLIM else std.os.freebsd.RLIM;
		pub const R_OK = if (@hasDecl(substitutes, "R_OK")) substitutes.R_OK else std.os.freebsd.R_OK;
		pub const S = if (@hasDecl(substitutes, "S")) substitutes.S else std.os.freebsd.S;
		pub const SA = if (@hasDecl(substitutes, "SA")) substitutes.SA else std.os.freebsd.SA;
		pub const SC = if (@hasDecl(substitutes, "SC")) substitutes.SC else std.os.freebsd.SC;
		pub const SEEK = if (@hasDecl(substitutes, "SEEK")) substitutes.SEEK else std.os.freebsd.SEEK;
		pub const SHUT = if (@hasDecl(substitutes, "SHUT")) substitutes.SHUT else std.os.freebsd.SHUT;
		pub const SIG = if (@hasDecl(substitutes, "SIG")) substitutes.SIG else std.os.freebsd.SIG;
		pub const SIOCGIFINDEX = if (@hasDecl(substitutes, "SIOCGIFINDEX")) substitutes.SIOCGIFINDEX else std.os.freebsd.SIOCGIFINDEX;
		pub const SO = if (@hasDecl(substitutes, "SO")) substitutes.SO else std.os.freebsd.SO;
		pub const SOCK = if (@hasDecl(substitutes, "SOCK")) substitutes.SOCK else std.os.freebsd.SOCK;
		pub const SOL = if (@hasDecl(substitutes, "SOL")) substitutes.SOL else std.os.freebsd.SOL;
		pub const STDERR_FILENO = if (@hasDecl(substitutes, "STDERR_FILENO")) substitutes.STDERR_FILENO else std.os.freebsd.STDERR_FILENO;
		pub const STDIN_FILENO = if (@hasDecl(substitutes, "STDIN_FILENO")) substitutes.STDIN_FILENO else std.os.freebsd.STDIN_FILENO;
		pub const STDOUT_FILENO = if (@hasDecl(substitutes, "STDOUT_FILENO")) substitutes.STDOUT_FILENO else std.os.freebsd.STDOUT_FILENO;
		pub const SYS = if (@hasDecl(substitutes, "SYS")) substitutes.SYS else std.os.freebsd.SYS;
		pub const Sigaction = if (@hasDecl(substitutes, "Sigaction")) substitutes.Sigaction else std.os.freebsd.Sigaction;
		pub const TCP = if (@hasDecl(substitutes, "TCP")) substitutes.TCP else std.os.freebsd.TCP;
		pub const TCSA = if (@hasDecl(substitutes, "TCSA")) substitutes.TCSA else std.os.freebsd.TCSA;
		pub const VDSO = if (@hasDecl(substitutes, "VDSO")) substitutes.VDSO else std.os.freebsd.VDSO;
		pub const W = if (@hasDecl(substitutes, "W")) substitutes.W else std.os.freebsd.W;
		pub const W_OK = if (@hasDecl(substitutes, "W_OK")) substitutes.W_OK else std.os.freebsd.W_OK;
		pub const X_OK = if (@hasDecl(substitutes, "X_OK")) substitutes.X_OK else std.os.freebsd.X_OK;
		pub const addrinfo = if (@hasDecl(substitutes, "addrinfo")) substitutes.addrinfo else std.os.freebsd.addrinfo;
		pub const blkcnt_t = if (@hasDecl(substitutes, "blkcnt_t")) substitutes.blkcnt_t else std.os.freebsd.blkcnt_t;
		pub const blksize_t = if (@hasDecl(substitutes, "blksize_t")) substitutes.blksize_t else std.os.freebsd.blksize_t;
		pub const clock_t = if (@hasDecl(substitutes, "clock_t")) substitutes.clock_t else std.os.freebsd.clock_t;
		pub const cpu_set_t = if (@hasDecl(substitutes, "cpu_set_t")) substitutes.cpu_set_t else std.os.freebsd.cpu_set_t;
		pub const dev_t = if (@hasDecl(substitutes, "dev_t")) substitutes.dev_t else std.os.freebsd.dev_t;
		pub const dl_phdr_info = if (@hasDecl(substitutes, "dl_phdr_info")) substitutes.dl_phdr_info else std.os.freebsd.dl_phdr_info;
		pub const empty_sigset = if (@hasDecl(substitutes, "empty_sigset")) substitutes.empty_sigset else std.os.freebsd.empty_sigset;
		pub const epoll_event = if (@hasDecl(substitutes, "epoll_event")) substitutes.epoll_event else std.os.freebsd.epoll_event;
		pub const fd_t = if (@hasDecl(substitutes, "fd_t")) substitutes.fd_t else std.os.freebsd.fd_t;
		pub const gid_t = if (@hasDecl(substitutes, "gid_t")) substitutes.gid_t else std.os.freebsd.gid_t;
		pub const ifreq = if (@hasDecl(substitutes, "ifreq")) substitutes.ifreq else std.os.freebsd.ifreq;
		pub const ino_t = if (@hasDecl(substitutes, "ino_t")) substitutes.ino_t else std.os.freebsd.ino_t;
		pub const mcontext_t = if (@hasDecl(substitutes, "mcontext_t")) substitutes.mcontext_t else std.os.freebsd.mcontext_t;
		pub const mode_t = if (@hasDecl(substitutes, "mode_t")) substitutes.mode_t else std.os.freebsd.mode_t;
		pub const msghdr = if (@hasDecl(substitutes, "msghdr")) substitutes.msghdr else std.os.freebsd.msghdr;
		pub const msghdr_const = if (@hasDecl(substitutes, "msghdr_const")) substitutes.msghdr_const else std.os.freebsd.msghdr_const;
		pub const nfds_t = if (@hasDecl(substitutes, "nfds_t")) substitutes.nfds_t else std.os.freebsd.nfds_t;
		pub const nlink_t = if (@hasDecl(substitutes, "nlink_t")) substitutes.nlink_t else std.os.freebsd.nlink_t;
		pub const off_t = if (@hasDecl(substitutes, "off_t")) substitutes.off_t else std.os.freebsd.off_t;
		pub const pid_t = if (@hasDecl(substitutes, "pid_t")) substitutes.pid_t else std.os.freebsd.pid_t;
		pub const pollfd = if (@hasDecl(substitutes, "pollfd")) substitutes.pollfd else std.os.freebsd.pollfd;
		pub const rlim_t = if (@hasDecl(substitutes, "rlim_t")) substitutes.rlim_t else std.os.freebsd.rlim_t;
		pub const rlimit = if (@hasDecl(substitutes, "rlimit")) substitutes.rlimit else std.os.freebsd.rlimit;
		pub const rlimit_resource = if (@hasDecl(substitutes, "rlimit_resource")) substitutes.rlimit_resource else std.os.freebsd.rlimit_resource;
		pub const rusage = if (@hasDecl(substitutes, "rusage")) substitutes.rusage else std.os.freebsd.rusage;
		pub const siginfo_t = if (@hasDecl(substitutes, "siginfo_t")) substitutes.siginfo_t else std.os.freebsd.siginfo_t;
		pub const sigset_t = if (@hasDecl(substitutes, "sigset_t")) substitutes.sigset_t else std.os.freebsd.sigset_t;
		pub const sockaddr = if (@hasDecl(substitutes, "sockaddr")) substitutes.sockaddr else std.os.freebsd.sockaddr;
		pub const socklen_t = if (@hasDecl(substitutes, "socklen_t")) substitutes.socklen_t else std.os.freebsd.socklen_t;
		pub const stack_t = if (@hasDecl(substitutes, "stack_t")) substitutes.stack_t else std.os.freebsd.stack_t;
		pub const tcflag_t = if (@hasDecl(substitutes, "tcflag_t")) substitutes.tcflag_t else std.os.freebsd.tcflag_t;
		pub const termios = if (@hasDecl(substitutes, "termios")) substitutes.termios else std.os.freebsd.termios;
		pub const time_t = if (@hasDecl(substitutes, "time_t")) substitutes.time_t else std.os.freebsd.time_t;
		pub const timespec = if (@hasDecl(substitutes, "timespec")) substitutes.timespec else std.os.freebsd.timespec;
		pub const timeval = if (@hasDecl(substitutes, "timeval")) substitutes.timeval else std.os.freebsd.timeval;
		pub const timezone = if (@hasDecl(substitutes, "timezone")) substitutes.timezone else std.os.freebsd.timezone;
		pub const ucontext_t = if (@hasDecl(substitutes, "ucontext_t")) substitutes.ucontext_t else std.os.freebsd.ucontext_t;
		pub const uid_t = if (@hasDecl(substitutes, "uid_t")) substitutes.uid_t else std.os.freebsd.uid_t;
		pub const user_desc = if (@hasDecl(substitutes, "user_desc")) substitutes.user_desc else std.os.freebsd.user_desc;
		pub const utsname = if (@hasDecl(substitutes, "utsname")) substitutes.utsname else std.os.freebsd.utsname;
		pub const PR = if (@hasDecl(substitutes, "PR")) substitutes.PR else std.os.freebsd.PR;
		pub const _errno = if (@hasDecl(substitutes, "_errno")) substitutes._errno else std.os.freebsd._errno;
		pub const Stat = if (@hasDecl(substitutes, "Stat")) substitutes.Stat else std.os.freebsd.Stat;
		pub const AI = if (@hasDecl(substitutes, "AI")) substitutes.AI else std.os.freebsd.AI;
		pub const NI = if (@hasDecl(substitutes, "NI")) substitutes.NI else std.os.freebsd.NI;
		pub const EAI = if (@hasDecl(substitutes, "EAI")) substitutes.EAI else std.os.freebsd.EAI;
		pub const fallocate64 = if (@hasDecl(substitutes, "fallocate64")) substitutes.fallocate64 else std.os.freebsd.fallocate64;
		pub const fopen64 = if (@hasDecl(substitutes, "fopen64")) substitutes.fopen64 else std.os.freebsd.fopen64;
		pub const fstat64 = if (@hasDecl(substitutes, "fstat64")) substitutes.fstat64 else std.os.freebsd.fstat64;
		pub const fstatat64 = if (@hasDecl(substitutes, "fstatat64")) substitutes.fstatat64 else std.os.freebsd.fstatat64;
		pub const ftruncate64 = if (@hasDecl(substitutes, "ftruncate64")) substitutes.ftruncate64 else std.os.freebsd.ftruncate64;
		pub const getrlimit64 = if (@hasDecl(substitutes, "getrlimit64")) substitutes.getrlimit64 else std.os.freebsd.getrlimit64;
		pub const lseek64 = if (@hasDecl(substitutes, "lseek64")) substitutes.lseek64 else std.os.freebsd.lseek64;
		pub const mmap64 = if (@hasDecl(substitutes, "mmap64")) substitutes.mmap64 else std.os.freebsd.mmap64;
		pub const open64 = if (@hasDecl(substitutes, "open64")) substitutes.open64 else std.os.freebsd.open64;
		pub const openat64 = if (@hasDecl(substitutes, "openat64")) substitutes.openat64 else std.os.freebsd.openat64;
		pub const pread64 = if (@hasDecl(substitutes, "pread64")) substitutes.pread64 else std.os.freebsd.pread64;
		pub const preadv64 = if (@hasDecl(substitutes, "preadv64")) substitutes.preadv64 else std.os.freebsd.preadv64;
		pub const pwrite64 = if (@hasDecl(substitutes, "pwrite64")) substitutes.pwrite64 else std.os.freebsd.pwrite64;
		pub const pwritev64 = if (@hasDecl(substitutes, "pwritev64")) substitutes.pwritev64 else std.os.freebsd.pwritev64;
		pub const sendfile64 = if (@hasDecl(substitutes, "sendfile64")) substitutes.sendfile64 else std.os.freebsd.sendfile64;
		pub const setrlimit64 = if (@hasDecl(substitutes, "setrlimit64")) substitutes.setrlimit64 else std.os.freebsd.setrlimit64;
		pub const getrandom = if (@hasDecl(substitutes, "getrandom")) substitutes.getrandom else std.os.freebsd.getrandom;
		pub const sched_getaffinity = if (@hasDecl(substitutes, "sched_getaffinity")) substitutes.sched_getaffinity else std.os.freebsd.sched_getaffinity;
		pub const eventfd = if (@hasDecl(substitutes, "eventfd")) substitutes.eventfd else std.os.freebsd.eventfd;
		pub const epoll_ctl = if (@hasDecl(substitutes, "epoll_ctl")) substitutes.epoll_ctl else std.os.freebsd.epoll_ctl;
		pub const epoll_create1 = if (@hasDecl(substitutes, "epoll_create1")) substitutes.epoll_create1 else std.os.freebsd.epoll_create1;
		pub const epoll_wait = if (@hasDecl(substitutes, "epoll_wait")) substitutes.epoll_wait else std.os.freebsd.epoll_wait;
		pub const epoll_pwait = if (@hasDecl(substitutes, "epoll_pwait")) substitutes.epoll_pwait else std.os.freebsd.epoll_pwait;
		pub const inotify_init1 = if (@hasDecl(substitutes, "inotify_init1")) substitutes.inotify_init1 else std.os.freebsd.inotify_init1;
		pub const inotify_add_watch = if (@hasDecl(substitutes, "inotify_add_watch")) substitutes.inotify_add_watch else std.os.freebsd.inotify_add_watch;
		pub const inotify_rm_watch = if (@hasDecl(substitutes, "inotify_rm_watch")) substitutes.inotify_rm_watch else std.os.freebsd.inotify_rm_watch;
		pub const getauxval = if (@hasDecl(substitutes, "getauxval")) substitutes.getauxval else std.os.freebsd.getauxval;
		pub const dl_iterate_phdr_callback = if (@hasDecl(substitutes, "dl_iterate_phdr_callback")) substitutes.dl_iterate_phdr_callback else std.os.freebsd.dl_iterate_phdr_callback;
		pub const dl_iterate_phdr = if (@hasDecl(substitutes, "dl_iterate_phdr")) substitutes.dl_iterate_phdr else std.os.freebsd.dl_iterate_phdr;
		pub const sigaltstack = if (@hasDecl(substitutes, "sigaltstack")) substitutes.sigaltstack else std.os.freebsd.sigaltstack;
		pub const memfd_create = if (@hasDecl(substitutes, "memfd_create")) substitutes.memfd_create else std.os.freebsd.memfd_create;
		pub const pipe2 = if (@hasDecl(substitutes, "pipe2")) substitutes.pipe2 else std.os.freebsd.pipe2;
		pub const fallocate = if (@hasDecl(substitutes, "fallocate")) substitutes.fallocate else std.os.freebsd.fallocate;
		pub const sendfile = if (@hasDecl(substitutes, "sendfile")) substitutes.sendfile else std.os.freebsd.sendfile;
		pub const copy_file_range = if (@hasDecl(substitutes, "copy_file_range")) substitutes.copy_file_range else std.os.freebsd.copy_file_range;
		pub const signalfd = if (@hasDecl(substitutes, "signalfd")) substitutes.signalfd else std.os.freebsd.signalfd;
		pub const prlimit = if (@hasDecl(substitutes, "prlimit")) substitutes.prlimit else std.os.freebsd.prlimit;
		pub const posix_memalign = if (@hasDecl(substitutes, "posix_memalign")) substitutes.posix_memalign else std.os.freebsd.posix_memalign;
		pub const malloc_usable_size = if (@hasDecl(substitutes, "malloc_usable_size")) substitutes.malloc_usable_size else std.os.freebsd.malloc_usable_size;
		pub const mincore = if (@hasDecl(substitutes, "mincore")) substitutes.mincore else std.os.freebsd.mincore;
		pub const madvise = if (@hasDecl(substitutes, "madvise")) substitutes.madvise else std.os.freebsd.madvise;
		pub const pthread_attr_t = if (@hasDecl(substitutes, "pthread_attr_t")) substitutes.pthread_attr_t else std.os.freebsd.pthread_attr_t;
		pub const pthread_mutex_t = if (@hasDecl(substitutes, "pthread_mutex_t")) substitutes.pthread_mutex_t else std.os.freebsd.pthread_mutex_t;
		pub const pthread_cond_t = if (@hasDecl(substitutes, "pthread_cond_t")) substitutes.pthread_cond_t else std.os.freebsd.pthread_cond_t;
		pub const pthread_rwlock_t = if (@hasDecl(substitutes, "pthread_rwlock_t")) substitutes.pthread_rwlock_t else std.os.freebsd.pthread_rwlock_t;
		pub const pthread_key_t = if (@hasDecl(substitutes, "pthread_key_t")) substitutes.pthread_key_t else std.os.freebsd.pthread_key_t;
		pub const sem_t = if (@hasDecl(substitutes, "sem_t")) substitutes.sem_t else std.os.freebsd.sem_t;
		pub const pthread_setname_np = if (@hasDecl(substitutes, "pthread_setname_np")) substitutes.pthread_setname_np else std.os.freebsd.pthread_setname_np;
		pub const pthread_getname_np = if (@hasDecl(substitutes, "pthread_getname_np")) substitutes.pthread_getname_np else std.os.freebsd.pthread_getname_np;
		pub const RTLD = if (@hasDecl(substitutes, "RTLD")) substitutes.RTLD else std.os.freebsd.RTLD;
		pub const dirent = if (@hasDecl(substitutes, "dirent")) substitutes.dirent else std.os.freebsd.dirent;
		pub const dirent64 = if (@hasDecl(substitutes, "dirent64")) substitutes.dirent64 else std.os.freebsd.dirent64;
		pub const whence_t = if (@hasDecl(substitutes, "whence_t")) substitutes.whence_t else std.os.freebsd.whence_t;
		pub const DIR = if (@hasDecl(substitutes, "DIR")) substitutes.DIR else std.os.freebsd.DIR;
		pub const opendir = if (@hasDecl(substitutes, "opendir")) substitutes.opendir else std.os.freebsd.opendir;
		pub const fdopendir = if (@hasDecl(substitutes, "fdopendir")) substitutes.fdopendir else std.os.freebsd.fdopendir;
		pub const rewinddir = if (@hasDecl(substitutes, "rewinddir")) substitutes.rewinddir else std.os.freebsd.rewinddir;
		pub const closedir = if (@hasDecl(substitutes, "closedir")) substitutes.closedir else std.os.freebsd.closedir;
		pub const telldir = if (@hasDecl(substitutes, "telldir")) substitutes.telldir else std.os.freebsd.telldir;
		pub const seekdir = if (@hasDecl(substitutes, "seekdir")) substitutes.seekdir else std.os.freebsd.seekdir;
		pub const clock_gettime = if (@hasDecl(substitutes, "clock_gettime")) substitutes.clock_gettime else std.os.freebsd.clock_gettime;
		pub const clock_getres = if (@hasDecl(substitutes, "clock_getres")) substitutes.clock_getres else std.os.freebsd.clock_getres;
		pub const gettimeofday = if (@hasDecl(substitutes, "gettimeofday")) substitutes.gettimeofday else std.os.freebsd.gettimeofday;
		pub const nanosleep = if (@hasDecl(substitutes, "nanosleep")) substitutes.nanosleep else std.os.freebsd.nanosleep;
		pub const getrusage = if (@hasDecl(substitutes, "getrusage")) substitutes.getrusage else std.os.freebsd.getrusage;
		pub const sched_yield = if (@hasDecl(substitutes, "sched_yield")) substitutes.sched_yield else std.os.freebsd.sched_yield;
		pub const sigaction = if (@hasDecl(substitutes, "sigaction")) substitutes.sigaction else std.os.freebsd.sigaction;
		pub const sigprocmask = if (@hasDecl(substitutes, "sigprocmask")) substitutes.sigprocmask else std.os.freebsd.sigprocmask;
		pub const sigfillset = if (@hasDecl(substitutes, "sigfillset")) substitutes.sigfillset else std.os.freebsd.sigfillset;
		pub const sigwait = if (@hasDecl(substitutes, "sigwait")) substitutes.sigwait else std.os.freebsd.sigwait;
		pub const socket = if (@hasDecl(substitutes, "socket")) substitutes.socket else std.os.freebsd.socket;
		pub const stat = if (@hasDecl(substitutes, "stat")) substitutes.stat else std.os.freebsd.stat;
		pub const alarm = if (@hasDecl(substitutes, "alarm")) substitutes.alarm else std.os.freebsd.alarm;
		pub const msync = if (@hasDecl(substitutes, "msync")) substitutes.msync else std.os.freebsd.msync;
		pub const fstat = if (@hasDecl(substitutes, "fstat")) substitutes.fstat else std.os.freebsd.fstat;
		pub const readdir = if (@hasDecl(substitutes, "readdir")) substitutes.readdir else std.os.freebsd.readdir;
		pub const realpath = if (@hasDecl(substitutes, "realpath")) substitutes.realpath else std.os.freebsd.realpath;
		pub const fstatat = if (@hasDecl(substitutes, "fstatat")) substitutes.fstatat else std.os.freebsd.fstatat;
		pub const getErrno = if (@hasDecl(substitutes, "getErrno")) substitutes.getErrno else std.os.freebsd.getErrno;
		pub const environ = if (@hasDecl(substitutes, "environ")) substitutes.environ else std.os.freebsd.environ;
		pub const fopen = if (@hasDecl(substitutes, "fopen")) substitutes.fopen else std.os.freebsd.fopen;
		pub const fclose = if (@hasDecl(substitutes, "fclose")) substitutes.fclose else std.os.freebsd.fclose;
		pub const fwrite = if (@hasDecl(substitutes, "fwrite")) substitutes.fwrite else std.os.freebsd.fwrite;
		pub const fread = if (@hasDecl(substitutes, "fread")) substitutes.fread else std.os.freebsd.fread;
		pub const printf = if (@hasDecl(substitutes, "printf")) substitutes.printf else std.os.freebsd.printf;
		pub const abort = if (@hasDecl(substitutes, "abort")) substitutes.abort else std.os.freebsd.abort;
		pub const exit = if (@hasDecl(substitutes, "exit")) substitutes.exit else std.os.freebsd.exit;
		pub const _exit = if (@hasDecl(substitutes, "_exit")) substitutes._exit else std.os.freebsd._exit;
		pub const isatty = if (@hasDecl(substitutes, "isatty")) substitutes.isatty else std.os.freebsd.isatty;
		pub const close = if (@hasDecl(substitutes, "close")) substitutes.close else std.os.freebsd.close;
		pub const lseek = if (@hasDecl(substitutes, "lseek")) substitutes.lseek else std.os.freebsd.lseek;
		pub const open = if (@hasDecl(substitutes, "open")) substitutes.open else std.os.freebsd.open;
		pub const openat = if (@hasDecl(substitutes, "openat")) substitutes.openat else std.os.freebsd.openat;
		pub const ftruncate = if (@hasDecl(substitutes, "ftruncate")) substitutes.ftruncate else std.os.freebsd.ftruncate;
		pub const raise = if (@hasDecl(substitutes, "raise")) substitutes.raise else std.os.freebsd.raise;
		pub const read = if (@hasDecl(substitutes, "read")) substitutes.read else std.os.freebsd.read;
		pub const readv = if (@hasDecl(substitutes, "readv")) substitutes.readv else std.os.freebsd.readv;
		pub const pread = if (@hasDecl(substitutes, "pread")) substitutes.pread else std.os.freebsd.pread;
		pub const preadv = if (@hasDecl(substitutes, "preadv")) substitutes.preadv else std.os.freebsd.preadv;
		pub const writev = if (@hasDecl(substitutes, "writev")) substitutes.writev else std.os.freebsd.writev;
		pub const pwritev = if (@hasDecl(substitutes, "pwritev")) substitutes.pwritev else std.os.freebsd.pwritev;
		pub const write = if (@hasDecl(substitutes, "write")) substitutes.write else std.os.freebsd.write;
		pub const pwrite = if (@hasDecl(substitutes, "pwrite")) substitutes.pwrite else std.os.freebsd.pwrite;
		pub const mmap = if (@hasDecl(substitutes, "mmap")) substitutes.mmap else std.os.freebsd.mmap;
		pub const munmap = if (@hasDecl(substitutes, "munmap")) substitutes.munmap else std.os.freebsd.munmap;
		pub const mprotect = if (@hasDecl(substitutes, "mprotect")) substitutes.mprotect else std.os.freebsd.mprotect;
		pub const link = if (@hasDecl(substitutes, "link")) substitutes.link else std.os.freebsd.link;
		pub const linkat = if (@hasDecl(substitutes, "linkat")) substitutes.linkat else std.os.freebsd.linkat;
		pub const unlink = if (@hasDecl(substitutes, "unlink")) substitutes.unlink else std.os.freebsd.unlink;
		pub const unlinkat = if (@hasDecl(substitutes, "unlinkat")) substitutes.unlinkat else std.os.freebsd.unlinkat;
		pub const getcwd = if (@hasDecl(substitutes, "getcwd")) substitutes.getcwd else std.os.freebsd.getcwd;
		pub const waitpid = if (@hasDecl(substitutes, "waitpid")) substitutes.waitpid else std.os.freebsd.waitpid;
		pub const wait4 = if (@hasDecl(substitutes, "wait4")) substitutes.wait4 else std.os.freebsd.wait4;
		pub const fork = if (@hasDecl(substitutes, "fork")) substitutes.fork else std.os.freebsd.fork;
		pub const access = if (@hasDecl(substitutes, "access")) substitutes.access else std.os.freebsd.access;
		pub const faccessat = if (@hasDecl(substitutes, "faccessat")) substitutes.faccessat else std.os.freebsd.faccessat;
		pub const pipe = if (@hasDecl(substitutes, "pipe")) substitutes.pipe else std.os.freebsd.pipe;
		pub const mkdir = if (@hasDecl(substitutes, "mkdir")) substitutes.mkdir else std.os.freebsd.mkdir;
		pub const mkdirat = if (@hasDecl(substitutes, "mkdirat")) substitutes.mkdirat else std.os.freebsd.mkdirat;
		pub const symlink = if (@hasDecl(substitutes, "symlink")) substitutes.symlink else std.os.freebsd.symlink;
		pub const symlinkat = if (@hasDecl(substitutes, "symlinkat")) substitutes.symlinkat else std.os.freebsd.symlinkat;
		pub const rename = if (@hasDecl(substitutes, "rename")) substitutes.rename else std.os.freebsd.rename;
		pub const renameat = if (@hasDecl(substitutes, "renameat")) substitutes.renameat else std.os.freebsd.renameat;
		pub const chdir = if (@hasDecl(substitutes, "chdir")) substitutes.chdir else std.os.freebsd.chdir;
		pub const fchdir = if (@hasDecl(substitutes, "fchdir")) substitutes.fchdir else std.os.freebsd.fchdir;
		pub const execve = if (@hasDecl(substitutes, "execve")) substitutes.execve else std.os.freebsd.execve;
		pub const dup = if (@hasDecl(substitutes, "dup")) substitutes.dup else std.os.freebsd.dup;
		pub const dup2 = if (@hasDecl(substitutes, "dup2")) substitutes.dup2 else std.os.freebsd.dup2;
		pub const readlink = if (@hasDecl(substitutes, "readlink")) substitutes.readlink else std.os.freebsd.readlink;
		pub const readlinkat = if (@hasDecl(substitutes, "readlinkat")) substitutes.readlinkat else std.os.freebsd.readlinkat;
		pub const chmod = if (@hasDecl(substitutes, "chmod")) substitutes.chmod else std.os.freebsd.chmod;
		pub const fchmod = if (@hasDecl(substitutes, "fchmod")) substitutes.fchmod else std.os.freebsd.fchmod;
		pub const fchmodat = if (@hasDecl(substitutes, "fchmodat")) substitutes.fchmodat else std.os.freebsd.fchmodat;
		pub const fchown = if (@hasDecl(substitutes, "fchown")) substitutes.fchown else std.os.freebsd.fchown;
		pub const umask = if (@hasDecl(substitutes, "umask")) substitutes.umask else std.os.freebsd.umask;
		pub const rmdir = if (@hasDecl(substitutes, "rmdir")) substitutes.rmdir else std.os.freebsd.rmdir;
		pub const getenv = if (@hasDecl(substitutes, "getenv")) substitutes.getenv else std.os.freebsd.getenv;
		pub const sysctl = if (@hasDecl(substitutes, "sysctl")) substitutes.sysctl else std.os.freebsd.sysctl;
		pub const sysctlbyname = if (@hasDecl(substitutes, "sysctlbyname")) substitutes.sysctlbyname else std.os.freebsd.sysctlbyname;
		pub const sysctlnametomib = if (@hasDecl(substitutes, "sysctlnametomib")) substitutes.sysctlnametomib else std.os.freebsd.sysctlnametomib;
		pub const tcgetattr = if (@hasDecl(substitutes, "tcgetattr")) substitutes.tcgetattr else std.os.freebsd.tcgetattr;
		pub const tcsetattr = if (@hasDecl(substitutes, "tcsetattr")) substitutes.tcsetattr else std.os.freebsd.tcsetattr;
		pub const fcntl = if (@hasDecl(substitutes, "fcntl")) substitutes.fcntl else std.os.freebsd.fcntl;
		pub const flock = if (@hasDecl(substitutes, "flock")) substitutes.flock else std.os.freebsd.flock;
		pub const ioctl = if (@hasDecl(substitutes, "ioctl")) substitutes.ioctl else std.os.freebsd.ioctl;
		pub const uname = if (@hasDecl(substitutes, "uname")) substitutes.uname else std.os.freebsd.uname;
		pub const gethostname = if (@hasDecl(substitutes, "gethostname")) substitutes.gethostname else std.os.freebsd.gethostname;
		pub const shutdown = if (@hasDecl(substitutes, "shutdown")) substitutes.shutdown else std.os.freebsd.shutdown;
		pub const bind = if (@hasDecl(substitutes, "bind")) substitutes.bind else std.os.freebsd.bind;
		pub const socketpair = if (@hasDecl(substitutes, "socketpair")) substitutes.socketpair else std.os.freebsd.socketpair;
		pub const listen = if (@hasDecl(substitutes, "listen")) substitutes.listen else std.os.freebsd.listen;
		pub const getsockname = if (@hasDecl(substitutes, "getsockname")) substitutes.getsockname else std.os.freebsd.getsockname;
		pub const getpeername = if (@hasDecl(substitutes, "getpeername")) substitutes.getpeername else std.os.freebsd.getpeername;
		pub const connect = if (@hasDecl(substitutes, "connect")) substitutes.connect else std.os.freebsd.connect;
		pub const accept = if (@hasDecl(substitutes, "accept")) substitutes.accept else std.os.freebsd.accept;
		pub const accept4 = if (@hasDecl(substitutes, "accept4")) substitutes.accept4 else std.os.freebsd.accept4;
		pub const getsockopt = if (@hasDecl(substitutes, "getsockopt")) substitutes.getsockopt else std.os.freebsd.getsockopt;
		pub const setsockopt = if (@hasDecl(substitutes, "setsockopt")) substitutes.setsockopt else std.os.freebsd.setsockopt;
		pub const send = if (@hasDecl(substitutes, "send")) substitutes.send else std.os.freebsd.send;
		pub const sendto = if (@hasDecl(substitutes, "sendto")) substitutes.sendto else std.os.freebsd.sendto;
		pub const sendmsg = if (@hasDecl(substitutes, "sendmsg")) substitutes.sendmsg else std.os.freebsd.sendmsg;
		pub const recv = if (@hasDecl(substitutes, "recv")) substitutes.recv else std.os.freebsd.recv;
		pub const recvfrom = if (@hasDecl(substitutes, "recvfrom")) substitutes.recvfrom else std.os.freebsd.recvfrom;
		pub const recvmsg = if (@hasDecl(substitutes, "recvmsg")) substitutes.recvmsg else std.os.freebsd.recvmsg;
		pub const kill = if (@hasDecl(substitutes, "kill")) substitutes.kill else std.os.freebsd.kill;
		pub const getdirentries = if (@hasDecl(substitutes, "getdirentries")) substitutes.getdirentries else std.os.freebsd.getdirentries;
		pub const setuid = if (@hasDecl(substitutes, "setuid")) substitutes.setuid else std.os.freebsd.setuid;
		pub const setgid = if (@hasDecl(substitutes, "setgid")) substitutes.setgid else std.os.freebsd.setgid;
		pub const seteuid = if (@hasDecl(substitutes, "seteuid")) substitutes.seteuid else std.os.freebsd.seteuid;
		pub const setegid = if (@hasDecl(substitutes, "setegid")) substitutes.setegid else std.os.freebsd.setegid;
		pub const setreuid = if (@hasDecl(substitutes, "setreuid")) substitutes.setreuid else std.os.freebsd.setreuid;
		pub const setregid = if (@hasDecl(substitutes, "setregid")) substitutes.setregid else std.os.freebsd.setregid;
		pub const setresuid = if (@hasDecl(substitutes, "setresuid")) substitutes.setresuid else std.os.freebsd.setresuid;
		pub const setresgid = if (@hasDecl(substitutes, "setresgid")) substitutes.setresgid else std.os.freebsd.setresgid;
		pub const malloc = if (@hasDecl(substitutes, "malloc")) substitutes.malloc else std.os.freebsd.malloc;
		pub const realloc = if (@hasDecl(substitutes, "realloc")) substitutes.realloc else std.os.freebsd.realloc;
		pub const free = if (@hasDecl(substitutes, "free")) substitutes.free else std.os.freebsd.free;
		pub const futimes = if (@hasDecl(substitutes, "futimes")) substitutes.futimes else std.os.freebsd.futimes;
		pub const utimes = if (@hasDecl(substitutes, "utimes")) substitutes.utimes else std.os.freebsd.utimes;
		pub const utimensat = if (@hasDecl(substitutes, "utimensat")) substitutes.utimensat else std.os.freebsd.utimensat;
		pub const futimens = if (@hasDecl(substitutes, "futimens")) substitutes.futimens else std.os.freebsd.futimens;
		pub const pthread_create = if (@hasDecl(substitutes, "pthread_create")) substitutes.pthread_create else std.os.freebsd.pthread_create;
		pub const pthread_attr_init = if (@hasDecl(substitutes, "pthread_attr_init")) substitutes.pthread_attr_init else std.os.freebsd.pthread_attr_init;
		pub const pthread_attr_setstack = if (@hasDecl(substitutes, "pthread_attr_setstack")) substitutes.pthread_attr_setstack else std.os.freebsd.pthread_attr_setstack;
		pub const pthread_attr_setstacksize = if (@hasDecl(substitutes, "pthread_attr_setstacksize")) substitutes.pthread_attr_setstacksize else std.os.freebsd.pthread_attr_setstacksize;
		pub const pthread_attr_setguardsize = if (@hasDecl(substitutes, "pthread_attr_setguardsize")) substitutes.pthread_attr_setguardsize else std.os.freebsd.pthread_attr_setguardsize;
		pub const pthread_attr_destroy = if (@hasDecl(substitutes, "pthread_attr_destroy")) substitutes.pthread_attr_destroy else std.os.freebsd.pthread_attr_destroy;
		pub const pthread_self = if (@hasDecl(substitutes, "pthread_self")) substitutes.pthread_self else std.os.freebsd.pthread_self;
		pub const pthread_join = if (@hasDecl(substitutes, "pthread_join")) substitutes.pthread_join else std.os.freebsd.pthread_join;
		pub const pthread_detach = if (@hasDecl(substitutes, "pthread_detach")) substitutes.pthread_detach else std.os.freebsd.pthread_detach;
		pub const pthread_atfork = if (@hasDecl(substitutes, "pthread_atfork")) substitutes.pthread_atfork else std.os.freebsd.pthread_atfork;
		pub const pthread_key_create = if (@hasDecl(substitutes, "pthread_key_create")) substitutes.pthread_key_create else std.os.freebsd.pthread_key_create;
		pub const pthread_key_delete = if (@hasDecl(substitutes, "pthread_key_delete")) substitutes.pthread_key_delete else std.os.freebsd.pthread_key_delete;
		pub const pthread_getspecific = if (@hasDecl(substitutes, "pthread_getspecific")) substitutes.pthread_getspecific else std.os.freebsd.pthread_getspecific;
		pub const pthread_setspecific = if (@hasDecl(substitutes, "pthread_setspecific")) substitutes.pthread_setspecific else std.os.freebsd.pthread_setspecific;
		pub const pthread_sigmask = if (@hasDecl(substitutes, "pthread_sigmask")) substitutes.pthread_sigmask else std.os.freebsd.pthread_sigmask;
		pub const sem_init = if (@hasDecl(substitutes, "sem_init")) substitutes.sem_init else std.os.freebsd.sem_init;
		pub const sem_destroy = if (@hasDecl(substitutes, "sem_destroy")) substitutes.sem_destroy else std.os.freebsd.sem_destroy;
		pub const sem_open = if (@hasDecl(substitutes, "sem_open")) substitutes.sem_open else std.os.freebsd.sem_open;
		pub const sem_close = if (@hasDecl(substitutes, "sem_close")) substitutes.sem_close else std.os.freebsd.sem_close;
		pub const sem_post = if (@hasDecl(substitutes, "sem_post")) substitutes.sem_post else std.os.freebsd.sem_post;
		pub const sem_wait = if (@hasDecl(substitutes, "sem_wait")) substitutes.sem_wait else std.os.freebsd.sem_wait;
		pub const sem_trywait = if (@hasDecl(substitutes, "sem_trywait")) substitutes.sem_trywait else std.os.freebsd.sem_trywait;
		pub const sem_timedwait = if (@hasDecl(substitutes, "sem_timedwait")) substitutes.sem_timedwait else std.os.freebsd.sem_timedwait;
		pub const sem_getvalue = if (@hasDecl(substitutes, "sem_getvalue")) substitutes.sem_getvalue else std.os.freebsd.sem_getvalue;
		pub const shm_open = if (@hasDecl(substitutes, "shm_open")) substitutes.shm_open else std.os.freebsd.shm_open;
		pub const shm_unlink = if (@hasDecl(substitutes, "shm_unlink")) substitutes.shm_unlink else std.os.freebsd.shm_unlink;
		pub const kqueue = if (@hasDecl(substitutes, "kqueue")) substitutes.kqueue else std.os.freebsd.kqueue;
		pub const kevent = if (@hasDecl(substitutes, "kevent")) substitutes.kevent else std.os.freebsd.kevent;
		pub const port_create = if (@hasDecl(substitutes, "port_create")) substitutes.port_create else std.os.freebsd.port_create;
		pub const port_associate = if (@hasDecl(substitutes, "port_associate")) substitutes.port_associate else std.os.freebsd.port_associate;
		pub const port_dissociate = if (@hasDecl(substitutes, "port_dissociate")) substitutes.port_dissociate else std.os.freebsd.port_dissociate;
		pub const port_send = if (@hasDecl(substitutes, "port_send")) substitutes.port_send else std.os.freebsd.port_send;
		pub const port_sendn = if (@hasDecl(substitutes, "port_sendn")) substitutes.port_sendn else std.os.freebsd.port_sendn;
		pub const port_get = if (@hasDecl(substitutes, "port_get")) substitutes.port_get else std.os.freebsd.port_get;
		pub const port_getn = if (@hasDecl(substitutes, "port_getn")) substitutes.port_getn else std.os.freebsd.port_getn;
		pub const port_alert = if (@hasDecl(substitutes, "port_alert")) substitutes.port_alert else std.os.freebsd.port_alert;
		pub const getaddrinfo = if (@hasDecl(substitutes, "getaddrinfo")) substitutes.getaddrinfo else std.os.freebsd.getaddrinfo;
		pub const freeaddrinfo = if (@hasDecl(substitutes, "freeaddrinfo")) substitutes.freeaddrinfo else std.os.freebsd.freeaddrinfo;
		pub const getnameinfo = if (@hasDecl(substitutes, "getnameinfo")) substitutes.getnameinfo else std.os.freebsd.getnameinfo;
		pub const gai_strerror = if (@hasDecl(substitutes, "gai_strerror")) substitutes.gai_strerror else std.os.freebsd.gai_strerror;
		pub const poll = if (@hasDecl(substitutes, "poll")) substitutes.poll else std.os.freebsd.poll;
		pub const ppoll = if (@hasDecl(substitutes, "ppoll")) substitutes.ppoll else std.os.freebsd.ppoll;
		pub const dn_expand = if (@hasDecl(substitutes, "dn_expand")) substitutes.dn_expand else std.os.freebsd.dn_expand;
		pub const PTHREAD_MUTEX_INITIALIZER = if (@hasDecl(substitutes, "PTHREAD_MUTEX_INITIALIZER")) substitutes.PTHREAD_MUTEX_INITIALIZER else std.os.freebsd.PTHREAD_MUTEX_INITIALIZER;
		pub const pthread_mutex_lock = if (@hasDecl(substitutes, "pthread_mutex_lock")) substitutes.pthread_mutex_lock else std.os.freebsd.pthread_mutex_lock;
		pub const pthread_mutex_unlock = if (@hasDecl(substitutes, "pthread_mutex_unlock")) substitutes.pthread_mutex_unlock else std.os.freebsd.pthread_mutex_unlock;
		pub const pthread_mutex_trylock = if (@hasDecl(substitutes, "pthread_mutex_trylock")) substitutes.pthread_mutex_trylock else std.os.freebsd.pthread_mutex_trylock;
		pub const pthread_mutex_destroy = if (@hasDecl(substitutes, "pthread_mutex_destroy")) substitutes.pthread_mutex_destroy else std.os.freebsd.pthread_mutex_destroy;
		pub const PTHREAD_COND_INITIALIZER = if (@hasDecl(substitutes, "PTHREAD_COND_INITIALIZER")) substitutes.PTHREAD_COND_INITIALIZER else std.os.freebsd.PTHREAD_COND_INITIALIZER;
		pub const pthread_cond_wait = if (@hasDecl(substitutes, "pthread_cond_wait")) substitutes.pthread_cond_wait else std.os.freebsd.pthread_cond_wait;
		pub const pthread_cond_timedwait = if (@hasDecl(substitutes, "pthread_cond_timedwait")) substitutes.pthread_cond_timedwait else std.os.freebsd.pthread_cond_timedwait;
		pub const pthread_cond_signal = if (@hasDecl(substitutes, "pthread_cond_signal")) substitutes.pthread_cond_signal else std.os.freebsd.pthread_cond_signal;
		pub const pthread_cond_broadcast = if (@hasDecl(substitutes, "pthread_cond_broadcast")) substitutes.pthread_cond_broadcast else std.os.freebsd.pthread_cond_broadcast;
		pub const pthread_cond_destroy = if (@hasDecl(substitutes, "pthread_cond_destroy")) substitutes.pthread_cond_destroy else std.os.freebsd.pthread_cond_destroy;
		pub const pthread_rwlock_destroy = if (@hasDecl(substitutes, "pthread_rwlock_destroy")) substitutes.pthread_rwlock_destroy else std.os.freebsd.pthread_rwlock_destroy;
		pub const pthread_rwlock_rdlock = if (@hasDecl(substitutes, "pthread_rwlock_rdlock")) substitutes.pthread_rwlock_rdlock else std.os.freebsd.pthread_rwlock_rdlock;
		pub const pthread_rwlock_wrlock = if (@hasDecl(substitutes, "pthread_rwlock_wrlock")) substitutes.pthread_rwlock_wrlock else std.os.freebsd.pthread_rwlock_wrlock;
		pub const pthread_rwlock_tryrdlock = if (@hasDecl(substitutes, "pthread_rwlock_tryrdlock")) substitutes.pthread_rwlock_tryrdlock else std.os.freebsd.pthread_rwlock_tryrdlock;
		pub const pthread_rwlock_trywrlock = if (@hasDecl(substitutes, "pthread_rwlock_trywrlock")) substitutes.pthread_rwlock_trywrlock else std.os.freebsd.pthread_rwlock_trywrlock;
		pub const pthread_rwlock_unlock = if (@hasDecl(substitutes, "pthread_rwlock_unlock")) substitutes.pthread_rwlock_unlock else std.os.freebsd.pthread_rwlock_unlock;
		pub const pthread_t = if (@hasDecl(substitutes, "pthread_t")) substitutes.pthread_t else std.os.freebsd.pthread_t;
		pub const FILE = if (@hasDecl(substitutes, "FILE")) substitutes.FILE else std.os.freebsd.FILE;
		pub const dlopen = if (@hasDecl(substitutes, "dlopen")) substitutes.dlopen else std.os.freebsd.dlopen;
		pub const dlclose = if (@hasDecl(substitutes, "dlclose")) substitutes.dlclose else std.os.freebsd.dlclose;
		pub const dlsym = if (@hasDecl(substitutes, "dlsym")) substitutes.dlsym else std.os.freebsd.dlsym;
		pub const sync = if (@hasDecl(substitutes, "sync")) substitutes.sync else std.os.freebsd.sync;
		pub const syncfs = if (@hasDecl(substitutes, "syncfs")) substitutes.syncfs else std.os.freebsd.syncfs;
		pub const fsync = if (@hasDecl(substitutes, "fsync")) substitutes.fsync else std.os.freebsd.fsync;
		pub const fdatasync = if (@hasDecl(substitutes, "fdatasync")) substitutes.fdatasync else std.os.freebsd.fdatasync;
		pub const prctl = if (@hasDecl(substitutes, "prctl")) substitutes.prctl else std.os.freebsd.prctl;
		pub const getrlimit = if (@hasDecl(substitutes, "getrlimit")) substitutes.getrlimit else std.os.freebsd.getrlimit;
		pub const setrlimit = if (@hasDecl(substitutes, "setrlimit")) substitutes.setrlimit else std.os.freebsd.setrlimit;
		pub const fmemopen = if (@hasDecl(substitutes, "fmemopen")) substitutes.fmemopen else std.os.freebsd.fmemopen;
		pub const syslog = if (@hasDecl(substitutes, "syslog")) substitutes.syslog else std.os.freebsd.syslog;
		pub const openlog = if (@hasDecl(substitutes, "openlog")) substitutes.openlog else std.os.freebsd.openlog;
		pub const closelog = if (@hasDecl(substitutes, "closelog")) substitutes.closelog else std.os.freebsd.closelog;
		pub const setlogmask = if (@hasDecl(substitutes, "setlogmask")) substitutes.setlogmask else std.os.freebsd.setlogmask;
		pub const if_nametoindex = if (@hasDecl(substitutes, "if_nametoindex")) substitutes.if_nametoindex else std.os.freebsd.if_nametoindex;
		pub const getcontext = if (@hasDecl(substitutes, "getcontext")) substitutes.getcontext else std.os.freebsd.getcontext;
		pub const max_align_t = if (@hasDecl(substitutes, "max_align_t")) substitutes.max_align_t else std.os.freebsd.max_align_t;
	};
}
